# user@host as window title
set-option -g set-titles on
set-option -g set-titles-string "#T"

# Trucolor terminal
set -g default-terminal "xterm-256color"
# From version 2.2 upwards
set-option -ga terminal-overrides ",xterm-256color:Tc"

# Activate mouse
set -g mouse on

# Vi keybindings
set -g mode-key vi
set -g status-key vi

# Bind Vim keys to window select
bind-key -r -T prefix       k                 select-pane -U
bind-key -r -T prefix       j                 select-pane -D
bind-key -r -T prefix       l                 select-pane -L
bind-key -r -T prefix       h                 select-pane -R

# Copy mode binding
unbind [
bind-key    -T prefix       ö                 copy-mode

# Copy with y to system clipboard
bind-key -T copy-mode-vi y                 send-keys -X copy-pipe-and-cancel "xclip -selection clipboard -i"
# For vi copy mode bindings
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "xclip -selection clipboard -i"
# For emacs copy mode bindings
bind-key -T copy-mode MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "xclip -selection clipboard -i"

# Rebind the prefix key to Ctrl-j -> this doesn't work well with vim
# set -g prefix C-j
# unbind C-b
# bind C-j send-prefix

# visuals
set -g status-style bg=blue

# have a look at https://gist.github.com/vincenthsu/6847a8f2a94e61735034e65d17ca0d66 for version check
#
# # Enable mouse control (clickable windows, panes, resizable panes) pre 2.1
# set -g mouse-select-window on
# set -g mouse-select-pane on
# set -g mouse-resize-pane on
